= form_for @shipping_weight do |f|
  - if @shipping_weight.errors.any?
    #error_explanation
      %h2= "#{pluralize(@shipping_weight.errors.count, "error")} prohibited this shipping_weight from being saved:"
      %ul
        - @shipping_weight.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
